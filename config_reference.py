
"""
实验室智能助手 - 配置文件详细说明
====================================

本文件提供了 config.json 中所有配置项的详细说明。
由于 JSON 格式不支持注释，请参考本文件了解各配置项的用途。

配置项说明：
"""

# =============================================================================
# 基础配置
# =============================================================================

CONFIG_EXAMPLE = {
    # =========================================================================
    # LLM 供应商配置
    # =========================================================================
    
    # LLM 供应商选择
    # 可选值: "openai" | "ollama"
    # 默认值: "openai"
    # 说明: 选择使用哪个大语言模型供应商
    "provider": "openai",
    
    # =========================================================================
    # 嵌入模型配置
    # =========================================================================
    
    # 嵌入模式
    # 可选值: "local" | "openai"
    # 默认值: "local"
    # 说明: 选择使用本地嵌入模型还是 OpenAI 云端嵌入
    "embedding_mode": "local",
    
    # 本地嵌入模型名称
    # 类型: string
    # 默认值: "BAAI/bge-small-zh-v1.5"
    # 说明: Hugging Face 模型 ID，用于本地嵌入
    # 推荐模型:
    #   - "BAAI/bge-small-zh-v1.5" (中文小模型，512维)
    #   - "BAAI/bge-base-zh-v1.5" (中文基础模型，768维)
    #   - "shibing624/text2vec-base-chinese" (轻量级中文模型)
    #   - "Qwen/Qwen3-Embedding-0.6B" (Qwen3 嵌入模型)
    "local_embedding_model": "BAAI/bge-small-zh-v1.5",
    
    # 本地模型缓存目录
    # 类型: string
    # 默认值: "" (空字符串表示使用默认目录)
    # 说明: 指定本地模型缓存的存储路径，留空使用 Hugging Face 默认缓存目录
    "local_model_cache_dir": "",
    
    # =========================================================================
    # OpenAI 配置
    # =========================================================================
    
    # OpenAI API 密钥
    # 类型: string
    # 必填: 当 provider 或 embedding_mode 为 "openai" 时
    # 说明: OpenAI 或兼容服务的 API 密钥
    "openai_api_key": "your-api-key-here",
    
    # OpenAI API 基础 URL
    # 类型: string
    # 默认值: "https://api.openai.com/v1"
    # 说明: OpenAI API 的基础 URL，可用于兼容第三方服务
    "openai_base_url": "https://api.openai.com/v1",
    
    # OpenAI 嵌入模型
    # 类型: string
    # 默认值: "text-embedding-3-small"
    # 说明: 使用的 OpenAI 嵌入模型名称
    "openai_embedding_model": "text-embedding-3-small",
    
    # OpenAI 聊天模型
    # 类型: string
    # 默认值: "gpt-3.5-turbo"
    # 说明: 使用的 OpenAI 聊天模型名称
    "openai_chat_model": "gpt-3.5-turbo",
    
    # =========================================================================
    # Ollama 配置
    # =========================================================================
    
    # Ollama 服务地址
    # 类型: string
    # 默认值: "http://localhost:11434"
    # 说明: Ollama 本地服务的访问地址
    "ollama_url": "http://localhost:11434",
    
    # Ollama 模型名称
    # 类型: string
    # 说明: 使用的 Ollama 模型名称，如 "qwen3:4b-instruct", "llama3:8b"
    "ollama_model": "qwen3:4b-instruct",
    
    # =========================================================================
    # 文档处理配置
    # =========================================================================
    
    # 文档分块大小（字符数）
    # 类型: integer
    # 默认值: 1500
    # 取值范围: 500 - 4000
    # 说明: 将文档切分为多个块时每个块的字符数
    # 注意: 过大可能导致上下文溢出，过小可能破坏语义完整性
    "chunk_size": 1500,
    
    # 分块重叠大小（字符数）
    # 类型: integer
    # 默认值: 100
    # 取值范围: 0 - 500
    # 说明: 相邻块之间的重叠字符数，用于保持上下文连续性
    "chunk_overlap": 100,
    
    # =========================================================================
    # LLM 生成参数
    # =========================================================================
    
    # 生成温度
    # 类型: float
    # 默认值: 0.7
    # 取值范围: 0.0 - 2.0
    # 说明: 控制输出的随机性，值越高越随机，值越低越确定
    "temperature": 0.7,
    
    # 检索返回的文档数量
    # 类型: integer
    # 默认值: 5
    # 取值范围: 1 - 20
    # 说明: 每次检索返回的相关文档 chunk 数量
    "top_k": 5,
    
    # 核采样参数 (top_p)
    # 类型: float
    # 默认值: 0.9
    # 取值范围: 0.0 - 1.0
    # 说明: 限制 token 采样的累积概率，与 temperature 配合使用
    "top_p": 0.9,
    
    # 最大生成 token 数
    # 类型: integer
    # 默认值: 2048
    # 取值范围: 256 - 8192
    # 说明: LLM 单次生成回答的最大 token 数量
    "max_tokens": 2048,
    
    # 存在惩罚 (presence_penalty)
    # 类型: float
    # 默认值: 0.0
    # 取值范围: -2.0 - 2.0
    # 说明: 减少模型重复相同内容的倾向，正值增加新话题
    "presence_penalty": 0.0,
    
    # 频率惩罚 (frequency_penalty)
    # 类型: float
    # 默认值: 0.0
    # 取值范围: -2.0 - 2.0
    # 说明: 根据 token 在文本中出现的频率进行惩罚，正值减少重复
    "frequency_penalty": 0.0,
    
    # =========================================================================
    # 对话历史配置
    # =========================================================================
    
    # 最大历史对话轮数
    # 类型: integer
    # 默认值: 10
    # 取值范围: 1 - 50
    # 说明: 保留的最大历史对话轮数，用于上下文管理
    "max_history": 10,
    
    # =========================================================================
    # 文件上传配置
    # =========================================================================
    
    # 上传文件最大大小（字节）
    # 类型: integer
    # 默认值: 104857600 (100MB)
    # 说明: 允许上传的单个文件最大大小（字节）
    # 常见大小参考:
    #   - 10MB: 10485760
    #   - 50MB: 52428800
    #   - 100MB: 104857600
    #   - 500MB: 524288000
    "upload_max_size": 104857600,
    
    # =========================================================================
    # 功能开关
    # =========================================================================
    
    # 是否启用思考步骤显示
    # 类型: boolean
    # 默认值: false
    # 说明: 是否显示 LLM 的思考过程（仅支持部分模型）
    "enable_thinking": false,
    
    # 是否允许用户注册
    # 类型: boolean
    # 默认值: false
    # 说明: 是否开放用户注册功能，生产环境建议关闭
    "allow_user_registration": false,
    
    # 是否启用 PDF 转换功能
    # 类型: boolean
    # 默认值: false
    # 说明: 是否将 PDF 转换为 Markdown 后再处理，false 则直接上传
    "allow_pdf_conversion": false,
    
    # =========================================================================
    # 混合检索配置
    # =========================================================================
    
    "hybrid_search": {
        # 是否启用混合检索
        # 类型: boolean
        # 默认值: true
        # 说明: 同时使用向量检索和 BM25 关键词检索
        "enabled": true,
        
        # 初始检索数量
        # 类型: integer
        # 默认值: 20
        # 说明: 每种检索方式初始返回的结果数量
        "initial_retrieve_count": 20,
        
        # 最终选择数量
        # 类型: integer
        # 默认值: 3
        # 说明: 重排序后最终选择的结果数量
        "final_select_count": 3,
        
        # BM25 检索权重
        # 类型: float
        # 默认值: 0.5
        # 取值范围: 0.0 - 1.0
        # 说明: BM25 检索结果在融合中的权重
        "bm25_weight": 0.5,
        
        # 向量检索权重
        # 类型: float
        # 默认值: 0.5
        # 取值范围: 0.0 - 1.0
        # 说明: 向量检索结果在融合中的权重
        "embedding_weight": 0.5,
        
        # 重排序模型
        # 类型: string
        # 默认值: "BAAI/bge-reranker-v2-m3"
        # 说明: 用于重排序的模型名称
        "rerank_model": "BAAI/bge-reranker-v2-m3",
        
        # 是否启用查询重写
        # 类型: boolean
        # 默认值: true
        # 说明: 是否使用 LLM 优化用户查询以提高检索准确率
        "enable_query_rewrite": true
    },
    
    # =========================================================================
    # 摘要检索配置
    # =========================================================================
    
    "summary_search": {
        # 是否启用摘要检索
        # 类型: boolean
        # 默认值: true
        # 说明: 启用双层检索（先检索摘要，再检索相关文档内容）
        "enabled": true,
        
        # 相关性阈值
        # 类型: float
        # 默认值: 0.6
        # 取值范围: 0.0 - 1.0
        # 说明: 摘要检索的相关性阈值，超过该值才被视为相关
        "relevance_threshold": 0.6,
        
        # 摘要检索数量
        # 类型: integer
        # 默认值: 5
        # 说明: 从摘要中检索的文档数量
        "summary_top_k": 5,
        
        # 内容检索数量
        # 类型: integer
        # 默认值: 3
        # 说明: 从相关文档内容中检索的 chunk 数量
        "content_top_k": 3,
        
        # 是否自动生成摘要
        # 类型: boolean
        # 默认值: true
        # 说明: 上传文档时是否自动生成文档摘要
        "auto_generate_summary": true
    },
    
    # =========================================================================
    # 上下文管理配置
    # =========================================================================
    
    "context_management": {
        # 是否启用上下文管理
        # 类型: boolean
        # 默认值: true
        # 说明: 是否管理对话历史上下文
        "enabled": true,
        
        # 最大历史轮数
        # 类型: integer
        # 默认值: 5
        # 说明: 保留的最大对话历史轮数
        "max_history_rounds": 5,
        
        # 是否排除错误消息
        # 类型: boolean
        # 默认值: true
        # 说明: 是否从上下文中排除错误消息
        "exclude_error_messages": true,
        
        # 是否排除可疑消息
        # 类型: boolean
        # 默认值: false
        # 说明: 是否从上下文中排除可疑质量的消息
        "exclude_questionable_messages": false
    },
    
    # =========================================================================
    # 技能系统配置
    # =========================================================================
    
    "skills": {
        # 是否启用技能系统
        # 类型: boolean
        # 默认值: true
        # 说明: 是否启用自定义技能功能
        "enabled": true,
        
        # ArXiv 搜索技能配置
        "arxiv_search": {
            # 是否启用 ArXiv 搜索
            # 类型: boolean
            # 默认值: true
            # 说明: 是否启用 ArXiv 论文搜索技能
            "enabled": true,
            
            # 最大结果数
            # 类型: integer
            # 默认值: 5
            # 说明: ArXiv 搜索返回的最大论文数量
            "max_results": 5
        }
    }
}

# =============================================================================
# 使用建议
# =============================================================================

"""
开发环境配置建议：
- provider: "ollama" (使用本地模型，节省 API 费用)
- embedding_mode: "local" (使用本地嵌入)
- enable_thinking: true (方便调试)

生产环境配置建议：
- provider: "openai" (稳定可靠)
- embedding_mode: "openai" (一致性更好)
- allow_user_registration: false (关闭注册)
- enable_thinking: false (减少输出)

性能优化建议：
- chunk_size: 1000-1500 (平衡上下文和语义)
- top_k: 3-5 (避免信息过载)
- 如内存不足，使用更小的嵌入模型
"""

